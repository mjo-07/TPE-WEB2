<?php

/** @var stdClass $editor */
?>

<form action="<?= ($editor) ? BASE_URL . 'addModifyEditor/' . $editor->id_editor : BASE_URL . 'altaEditor' ?>" method="POST" enctype="multipart/form-data" class="container mt-4 p-4 border rounded bg-light shadow-sm">

    <h2 class="mb-4 text-primary">Formulario de Editor</h2>

    <div class="row">
        <div class="col-md-6 mb-3">
            <label for="nombre_empresa" class="form-label fw-bold">Nombre de la Empresa *</label>
            <input value="<?= htmlspecialchars($editor->nombre_empresa ?? '') ?>" type="text" class="form-control" id="nombre_empresa" name="nombre_empresa" placeholder="Ej. Nintendo, Sony, EA" required maxlength="50">
        </div>

        <div class="col-md-6 mb-3">
            <label for="pais" class="form-label fw-bold">País de Origen *</label>
            <input type="text" class="form-control" id="pais" name="pais" placeholder="Ej. Japón, EE.UU." required maxlength="20" value="<?= htmlspecialchars($editor->pais ?? '') ?>">
        </div>
    </div>

    <div class="row">
        <div class="col-md-8 mb-3">
            <label for="sitio_web" class="form-label fw-bold">Sitio Web Oficial *</label>
            <input type="url" class="form-control" id="sitio_web" name="sitio_web" placeholder="https://ejemplo.com" required maxlength="100" value="<?= htmlspecialchars($editor->sitio_web ?? '') ?>">
        </div>

        <div class="col-md-4 mb-3">
            <label for="valoracion" class="form-label fw-bold">Valoración (0 a 5)</label>
            <input type="number" class="form-control" id="valoracion" name="valoracion" placeholder="Ej. 5" min="0" max="5" value="<?= $editor->valoracion ?? '' ?>">
        </div>
    </div>

    <div class="mb-3">
        <label for="descripcion" class="form-label fw-bold">Descripción / Historia</label>
        <textarea class="form-control" id="descripcion" name="descripcion" rows="3" placeholder="Breve reseña de la empresa..."><?= htmlspecialchars($editor->descripcion ?? '') ?></textarea>
    </div>

    <div class="mb-4">
        <label for="imagen" class="form-label fw-bold">Imagen o Logo del Editor</label>

        <?php if (isset($editor) && !empty($editor->imagen)): ?>
            <div class="mb-3 p-2 border rounded bg-white text-center d-inline-block">
                <p class="small text-muted mb-1">Imagen actual:</p>
                <img src="<?= BASE_URL ?>imagenes/<?= $editor->imagen ?? 'errores/imgSinCargar.png' ?>" alt="Logo actual" class="img-thumbnail" style="max-height: 100px;">

                <input type="hidden" name="imagen_actual" value="<?= $editor->imagen ?>">
            </div>
        <?php endif; ?>

        <input class="form-control" type="file" id="imagen" name="imagen" accept="image/*">
        <div class="form-text">Formatos sugeridos: JPG, PNG, WEBP. Déjalo vacío para mantener la imagen actual.</div>
    </div>

    <div class="d-flex justify-content-end gap-2">
        <a href="<?= BASE_URL ?>/editEditores" class="btn btn-secondary">Cancelar</a>
        <button type="submit" class="btn btn-success"><?= ($editor) ? 'Modificar editor' : 'Guardar Editor' ?></button>
    </div>

</form>